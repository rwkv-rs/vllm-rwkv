# Dense RWKV7 inference and OpenAI-compatible text serving on CUDA.
#
# This profile intentionally excludes attention, MLA, MoE, multimodal,
# quantized-loader, and speculative-decoding dependencies.

torch==2.13.0

# Core configuration, model loading, tokenizer, and process runtime.
regex
cachetools
psutil
numpy
requests>=2.26.0
tqdm
blake3
py-cpuinfo
transformers>=5.5.3
tokenizers>=0.21.1
safetensors>=0.6.2
protobuf>=5.29.6,!=6.30.*,!=6.31.*,!=6.32.*,!=6.33.0.*,!=6.33.1.*,!=6.33.2.*,!=6.33.3.*,!=6.33.4.*
typing_extensions>=4.10
filelock>=3.16.1
pyyaml
cloudpickle
packaging>=24.2
jinja2>=3.1.6
pillow

# OpenAI-compatible server and metrics.
fastapi[standard]>=0.133.0,<0.137.0
starlette>=1.0.1
aiohttp>=3.13.3
openai>=2.0.0
openai-harmony>=0.0.3
model-hosting-container-standards>=0.1.14,<1.0.0
partial-json-parser
pydantic>=2.12.0
prometheus_client>=0.18.0
prometheus-fastapi-instrumentator>=8.0.0
pyzmq>=25.0.0
msgspec
watchfiles
python-json-logger
setproctitle
pybase64
cbor2

# Source build dependencies.
cmake>=3.26.1
ninja
setuptools>=77.0.3,<81.0.0
setuptools-scm>=8.0
wheel
